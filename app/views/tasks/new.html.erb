<h1>New task</h1>

<%= form_for([@current_user, @task]) do |f| %>
  <%= render 'form', :f => f %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', user_tasks_path(@current_user) %>
